describe Fastlane::Actions::Postmark do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The plugin postmark is working!")

      # Fastlane::Actions::Postmark.run(nil)
    end
  end
end
